{
  "name": "vdo-video-to-blog",
  "version": "1.0.0",
  "description": "Video to Blog AI Converter",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../ai-service && pip install -r requirements.txt"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0",
    "axios": "^1.4.0",
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": "18.x"
  }
}
